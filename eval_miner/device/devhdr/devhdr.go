package devhdr

const MaxHashBoards = 3 // CONFIGURE per system
const FanFileDir = "/tmp/fan/"
const EvalHashBoardId = 2 // CONFIGURE per system
const EvalThs = 40.0
const NumChips = 132      // CONFIGURE per system
const DiffMin = 32        // CONFIGURE per system
const DiffMax = 16 * 1024 // CONFIGURE per system
